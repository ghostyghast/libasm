bits 64

section .data
msg:	db "Hello, World!", 10, 0
msg_len equ $ - msg

section .text
global _start

_start: 
	; sys_write
	mov rax, 1
	mov rdi, 1
	mov rsi, msg
	mov rdx, msg_len
	syscall

	; sys_exit
	mov rax, 60 
	mov rdi, 50
	syscall
